DATABASE_URL=postgres://lobby:lobby@localhost:5432/lobby
JWT_SECRET=dev-secret-change-me
STEAM_API_KEY=test
STEAM_APP_ID=480
MATCH_ACCEPT_TIMEOUT_S=30
REPORT_TIMEOUT_S=300
RUST_LOG=info,lobby_server=debug
LOBBY_HOST=0.0.0.0
LOBBY_PORT=8080
# Public origin for OpenID login redirects (e.g. https://lobby.example.com).
# Unset: only relative return_to URLs are accepted; absolute ones are rejected.
PUBLIC_URL=
